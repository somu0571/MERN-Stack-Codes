// let n = "   hello    ";
// n.trim();  // using trim method to have whitespaces

let k = "Sourav"
k.toLowerCase();  // to lower case the string
k.toUpperCase(); // to upper case the string
k.indexOf("o");  // string method with arguments, indexOf use to find index of a specific element
// Method Chaining, using one method after another. Order of execution will be left to right
let s = "  amogh  ";
s.toUpperCase().trim();
let j = "I love coding";
let p = j.slice(0,4); // slicing
console.log(p);

let m = "somnath";
let mp = m.replace("mna","sum"); // replace the string
let kcc = m.repeat(3); // repeatation of the string
console.log(mp);
console.log(kcc);
