const item = {
    price: 100.99,
    discount: 50,
    colors: ['red','pink']
};

// to access the value we write it as item["price"] or item.price

// Conversion in Get Values
const obj = {
    1: "a",
    2: "b",
    true: "c",
    null: "d",
    undefined: "e"
};

// Add/Update Value :- to update we can write as for eg. person["city"] = "Mumbai", then it will update it to Mumbai , 
// the same process is to add new key value pair as well
// to delete :- we can write it as (delete person.marks; )
const person = {
    name: "Sourav",
    age: 29,
    city: "Delhi",
    marks: 94.4
};